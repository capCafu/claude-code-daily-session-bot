services:
  bot:
    build: .
    env_file: .env
    volumes:
      - ./data:/data
      - ./data/claude-home/.claude:/root/.claude
      - ./data/claude-home/.claude.json:/root/.claude.json
    restart: unless-stopped
